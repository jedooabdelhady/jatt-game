# 🎲 جاطت - دليل التشغيل على الأجهزة المحمولة

## إعدادات التشغيل

### على الويب/الويب المحلي
```bash
npm install
npm start
# السيرفر يعمل على http://localhost:3000
```

### على الهاتف (الشبكة المحلية)
1. أوجد عنوان IP للكمبيوتر:
   ```bash
   ipconfig
   # ابحث عن IPv4 Address (مثل 192.168.x.x)
   ```

2. على الهاتف، افتح:
   ```
   http://[your-ip]:3000
   ```

---

## اقتراحات لـ iOS و Android

### الأداء
- **حجم الشاشة**: الواجهة مُحسّنة للهواتف (width: 480px max). تعمل على شاشات أكبر لكن مثالي للهاتف.
- **الذاكرة**: تُنظّف الرسائل القديمة (> 50 رسالة) تلقائياً كل 5 ثوانٍ لمنع تسريب الذاكرة.
- **الحركة**: تطبيق تنسيق CSS الحديث (transform, transition) لسلاسة الحركة.

### الصوت (تم تعطيله حالياً)
- الأصوات تم حذفها من الكود لتجنب مشاكل autoplay على الهواتف.
- إذا أردت إضافة أصوات لاحقاً، استخدم آلية "تمكين الصوت" عند أول نقرة للاعب.

### الشبكة
- **Socket.IO**: يستخدم websockets (مع fallback إلى polling).
- **الكمون (Latency)**: التايمر متزامن مع السيرفر (server-side timeout).
- **إعادة الاتصال**: تلقائياً عند قطع الشبكة؛ حالة اللعبة تُستوعد من السيرفر.

---

## الأخطاء الشائعة وحلولها

### 1. "الكود غلط يا فنان!"
- **السبب**: الكود المدخل لا يوجد في السيرفر.
- **الحل**: تأكد من نسخ الكود كاملاً (4 أرقام)، أو أعد المضيف انضمام الكود.

### 2. "الغرفة ممتلئة!"
- **السبب**: الحد الأقصى من اللاعبين وصل (8 افتراضياً).
- **الحل**: أنشئ غرفة جديدة أو انتظر حتى يغادر لاعب.

### 3. التايمر لا يعمل بنفس السرعة الغرفة/الجهاز
- **السبب**: الكمون العالي (شبكة بطيئة) أو فرق في الساعات.
- **الحل**: 
  - السيرفر يقطع الجولة بعد الوقت المحدد + 1 ثانية (buffer).
  - الكلاينت يحسب الوقت المتبقي بناءً على `startTime` من السيرفر.
  - **النتيجة**: أي اختلاف في الشاشة لن يؤثر على النتيجة (المركزي = السيرفر).

### 4. "بانتظار المضيف..." - التحميل لا ينتهي
- **السبب**: اتصال ضعيف أو المضيف في حالة غير متوقعة.
- **الحل**:
  - اضغط ← الرجوع في الهاتف و "إعادة الدخول".
  - إذا استمر، أغلق التطبيق و أعد الدخول.

### 5. الشات لا يظهر / يختفي
- **السبب**: الشات قابل للسحب (draggable)؛ قد تسحبه خارج الشاشة.
- **الحل**: اضغط الزر "💬" (أسفل يسار) لإعادة فتحه. إذا اختفى تماماً، أعد تحميل الصفحة.

### 6. القطع من الشبكة أثناء الجولة
- **ما يحدث**: 
  - السيرفر ينتظر 5 ثواني قبل حذف اللاعب من الغرفة.
  - إذا عادت الاتصال خلال 5 ثوانٍ، اضغط "إعادة الدخول" على الهاتف.
- **التأثير**: الإجابة أو التصويت لن يُحفظ إذا قطعت قبل الإرسال.

---

## اختبار على الهاتف الحقيقي

### المتطلبات
- كمبيوتر وهاتف على **نفس الشبكة المحلية (WiFi)**.
- السيرفر يعمل على الكمبيوتر.

### الخطوات
1. شغّل السيرفر:
   ```bash
   npm start
   ```

2. على الهاتف، افتح:
   ```
   http://192.168.x.x:3000
   ```
   (استبدل `192.168.x.x` بـ IP الكمبيوتر الفعلي)

3. اختبر:
   - إنشاء غرفة وانضمام آخر.
   - اختيار موضوع وإجابة.
   - التصويت على الإجابات.
   - اختبر قطع الشبكة wiFI مؤقتاً وأعد الاتصال.

---

## ملاحظات الأداء

| المقياس | القيمة |
|--------|--------|
| حد اللاعبين الأقصى | 8 لاعبين |
| وقت الجولة الافتراضي | 30 ثانية |
| وقت التصويت | افتراضي 30 ثانية |
| تنظيف الذاكرة | كل 5 ثوانٍ |
| حد الرسائل في الشات | 50 رسالة |
| تحديد معدل الانضمام | 1 ثانية بين محاولات |

---

## ملاحظات الأمان

- **Rate limiting**: 1 ثانية بين محاولات الدخول (منع spam).
- **التحقق من الإجابات**: تُقارن الإجابة بالحقيقة باستخدام `normalizeText()` (يتجاهل التشكيل والأخطاء الإملائية البسيطة).
- **منع التصويت للنفس**: مراجعة على السيرفر والكلاينت.
- **تنظيف الموارد**: الغرف الفارغة تُحذف تلقائياً.

---

## قائمة تحقق النشر (Deployment Checklist)

- [ ] تشغيل الاختبارات الآلية (`npm test` أو `node test_clients.js`).
- [ ] اختبار على نطاق محلي بـ 4-8 لاعبين.
- [ ] اختبار قطع الشبكة والإعادة.
- [ ] اختبار الطرد وإعادة الضبط.
- [ ] تحقق من السجلات (logs) للأخطاء غير المتوقعة.
- [ ] تأكد من عدم وجود تسريب الذاكرة (راقب استهلاك الـ RAM بعد ساعة من التشغيل).

---

جاهز للبدء! 🚀
